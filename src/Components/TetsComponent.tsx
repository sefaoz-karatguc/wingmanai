import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const TetsComponent = (props: Props) => {
  return (
    <View>
      <Text>TetsComponent</Text>
    </View>
  );
};

export default TetsComponent;

const styles = StyleSheet.create({});
